spring:
  application:
    name: resource-service
  datasource:
    url: jdbc:postgresql://${DB_HOST:postgres}:${DB_PORT:5432}/${DB_NAME:sarc}?currentSchema=sarc_resources
    username: ${DB_USER:sarc}
    password: ${DB_PASSWORD:sarc}
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false
    properties:
      hibernate:
        default_schema: sarc_resources
  flyway:
    enabled: true
    schemas: sarc_resources
    locations: classpath:db/migration
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ${KEYCLOAK_EXTERNAL_URL:http://localhost:8090}/realms/sarc
          jwk-set-uri: ${KEYCLOAK_INTERNAL_URL:http://keycloak:8080}/realms/sarc/protocol/openid-connect/certs

server:
  port: 8083
